<?xml version="1.0" encoding="UTF-8"?>
<project name="" basedir="." default="...">




    <!-- ==================================== -->
    <!--              Properties              -->
    <!-- ==================================== -->




    <!-- Set properties here -->




    <!-- ==================================== -->
    <!--             Dependencies             -->
    <!-- ==================================== -->




    <import file="${phingy.path}/scripts/platform/laravel.xml" />




    <!-- ==================================== -->
    <!--                Tasks                 -->
    <!-- ==================================== -->




    <target name="project:config" depends="laravel:config" hidden="true">
        <echo msg="Running project:config" />
    </target>


    <target name="project:build:before" depends="laravel:build:before" hidden="true">
        <echo msg="Running project:build:before" />
    </target>


    <target name="project:build:after" depends="laravel:build:after" hidden="true">
        <echo msg="Running project:build:after" />
    </target>


    <target name="project:build:housekeeping" depends="laravel:build:housekeeping" hidden="true">
        <echo msg="Running project:build:housekeeping" />
    </target>




</project>
